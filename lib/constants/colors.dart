import 'package:flutter/material.dart';

class AppColors {
  static const blurContainer = LinearGradient(colors: [
    Color.fromRGBO(255, 255, 255, 0.2),
    Color.fromRGBO(255, 255, 255, 0.2)
  ]);

  static const borderColor = Colors.black;
  static const containerTransparent = Colors.transparent;
  static const rowTextColor = Colors.grey;
}
